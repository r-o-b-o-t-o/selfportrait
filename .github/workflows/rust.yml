name: Rust

on: [push]

jobs:
    build:
        runs-on: ubuntu-latest

        steps:
        - uses: actions/checkout@v1
          with:
            rust-version: 1.50
        - name: Build
          run: cargo build --verbose
        - name: Run tests
          run: cargo test --verbose
